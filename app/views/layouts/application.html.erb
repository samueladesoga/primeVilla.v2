<!DOCTYPE html>
<html>
  <head>
     <%= stylesheet_link_tag 'tailwind', "data-turbo-track": "reload" %>
    <title><%= content_for(:title) || "Prime Villa" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :application, "data-turbo-track": "reload" %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100 text-gray-800 font-sans">
    <header class="bg-white shadow p-4 mb-6">
      <div class="container mx-auto flex justify-between items-center">
        <h1 class="text-xl font-bold text-blue-600">Prime Villa</h1>
        <% if Current.user.present? %>
          <p>Welcome, <%= Current.user.name %></p>
          <%= button_to "Logout", logout_path, method: :delete, class: "text-red-500", form: { data: { turbo: false } } %>
        <% else %>
          <%= link_to "Login", login_path, class: "text-blue-500" %>
        <% end %>
      </div>
    </header>

    <main class="container mx-auto px-4">
      <%= render "layouts/navbar" %>
      <%= tag.div(flash[:alert], style: "color:red") if flash[:alert] %>
      <%= tag.div(flash[:notice], style: "color:green") if flash[:notice] %>
      <%= yield %>
    </main>
  </body>
</html>
